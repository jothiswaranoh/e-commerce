source "https://rubygems.org"

ruby ">= 3.1.0" # Ensure your Ruby version is specified

gem "rails", "~> 8.0.1"

# Database
gem "pg", "~> 1.6"

# Web Server
gem "puma", ">= 5.0"

# Authentication & Authorization
gem "bcrypt", "~> 3.1.7"
gem "cancancan"
gem "jwt"
gem "rack-cors"

# API Documentation (Moved out of development/test group to fix NameError)
gem "rswag"

# Pagination and Serialization
gem "blueprinter"
gem "will_paginate", "~> 3.3"

# Windows support
gem "tzinfo-data", platforms: %i[ windows jruby ]

# Rails 8+ performance and storage defaults
gem "solid_cache"
gem "solid_queue"
gem "solid_cable"
gem "bootsnap", require: false

# Deployment
gem "kamal", require: false

# --- Environment Specific Groups ---

group :development, :test do
  # Debugging tools (Kept only one entry to fix your log warning)
  gem "debug", platforms: %i[mri windows], require: false

  # Static analysis and code quality
  #
  gem "brakeman", require: false
  gem "rubocop-rails-omakase", require: false

  # Console enhancements
  gem "awesome_print"
  gem "pry"
  gem "pry-byebug"

  # API testing specs
  gem "rswag-specs"
end

group :development do
  # Add development-only gems here
end